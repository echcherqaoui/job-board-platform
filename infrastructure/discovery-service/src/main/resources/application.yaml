spring:
  application:
    name: discovery-service

server:
  port: ${DISCOVERY_PORT}

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

management:
  server:
    port: ${MANAGEMENT_PORT}
  endpoints:
    web:
      exposure:
        include: health, info